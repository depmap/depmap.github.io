html
  head
    meta(charset='utf-8')
    meta(http-equiv='X-UA-Compatible' content='IE=edge, chrome=1')
    meta(name='description' content='')
    meta(name='author' content='')
    meta(name='viewport' content='width=device-width, initial-scale=1')

    title Depmap - Why compile more?
    link(rel='stylesheet' type='text/css' href='css/index.css')
    link(href='https://fonts.googleapis.com/css?family=Source+Code+Pro|Fira+Sans|Roboto' rel='stylesheet')

    body
      header
        .info
          a.fork-me(href='//github.com/depmap/depmap') Fork Me

          h1 Depmap
          h2 Why compile more?
          h3 Reduce compile time by only compiling changes.

      #blocks
        .block#installation
          h3 Installation
          pre: code $ npm install -g @depmap/cli

        .block#quickstart
          h3 Quickstart
          p Clone this repository, install pkgs, start 
            span.snippet depmap
            span , and edit a file in 
            span.snippet src/
          pre: code
            | $ git clone git@github.com:depmap/depmap.github.io.git
            | $ cd depmap.github.io
            | $ npm install
            | $ depmap watch

        .block#usage
          h3 Usage

          h4 Compile Once
          pre: code $ depmap compile

          h4 Watch Continously
          pre: code $ depmap watch

        .block#configuration
          h3 Configuration
          p Add language 
            span.snippet loaders
            | , set your 
            span.snippet path
            |  & 
            span.snippet output
            | , and start working
          pre: code
            | // config.js
            | const pug  = require('@depmap/pug-loader')
            | const styl = require('@depmap/stylus-loader')
            | const js   = require('@depmap/babel-loader')
            | const img  = require('@depmap/img-loader')
            |
            | module.exports = {
            |   path: 'src/**/*',
            |   output: 'build',
            |   load: { pug, styl, js, img }
            | }
